---
import SectionHeading from './SectionHeading.astro';
import ServiceCard from './ServiceCard.astro';

const services = [
  {
    id: 'service-mental-health',
    title: 'メンタルヘルス研修',
    body: '不調の早期発見とセルフケア、相談しやすい職場づくりを支援します',
    bodyTone: 'dark' as const,
    icon: '/images/service-icon-heart.svg',
    ctaLabel: 'メンタルヘルス研修',
  },
  {
    id: 'service-anger-management',
    title: 'アンガーマネジメント研修',
    body: '怒りを抑えるのではなく、上手に扱い、冷静に対処できる力を身につけます',
    bodyTone: 'muted' as const,
    icon: '/images/service-icon-fire.svg',
    ctaLabel: 'アンガーマネジメント研修',
  },
  {
    id: 'service-harassment',
    title: 'ハラスメント研修',
    body: 'ハラスメントによるトラブルに対して、ひとりで抱え込まない対応と組織としての線引きを学びます',
    bodyTone: 'muted' as const,
    icon: '/images/service-icon-prohibited.svg',
    ctaLabel: 'ハラスメント研修',
  },
];
---
<section id="services" class="bg-white pt-[130px] pb-[80px]" data-reveal>
  <div class="mx-auto w-full min-w-0 max-w-pageShell px-6">
    <div class="mx-auto w-full min-w-0 max-w-page">
    <SectionHeading label="SERVICE" title="研修サービス一覧" decoLoading="eager">
      <div
        slot="after"
        class="mt-2 max-w-[720px] text-base font-zen font-normal leading-[1.6] text-black-03"
      >
        <p class="mb-0">感情労働で働く皆さまがイキイキと働くために、３つの研修をご用意しています</p>
        <p>課題に合わせて、研修内容はカスタマイズいたします</p>
      </div>
    </SectionHeading>

    <!-- SP のみ（md:hidden）: カルーセル操作域のため親 px-6 を相殺。PCでは非表示のためレイアウト仕様書のフルブリード禁止に該当しない -->
    <div
      class="service-embla-sp mt-4 w-[calc(100%+3rem)] min-w-0 max-w-none shrink-0 -mx-6 py-4 pb-5 md:hidden"
    >
      <div class="min-w-0 w-full px-0">
        <div
          id="service-embla-viewport"
          class="service-embla__viewport min-w-0 overflow-hidden py-2"
          role="region"
          aria-roledescription="カルーセル"
          aria-label="研修サービス一覧。横にスワイプして全件を表示できます"
          tabindex="0"
        >
          <div id="service-embla-container" class="service-embla__container flex">
            {
              services.map((s) => (
                <div class="service-embla__slide flex shrink-0 grow-0 py-1">
                  <ServiceCard {...s} />
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>

    <!-- md+: 3 カラムグリッド（アンカー id はこちらのみ） -->
    <div
      id="service-carousel"
      class="relative mt-10 hidden min-w-0 w-full grid-cols-3 gap-x-[27px] gap-y-0 items-stretch md:grid"
      aria-label="研修サービス一覧"
    >
      {
        services.map((s) => (
          <div class="min-w-0">
            <ServiceCard {...s} anchorId={s.id} />
          </div>
        ))
      }
    </div>
    </div>
  </div>
</section>

<style is:global>
  .service-embla__viewport {
    touch-action: pan-y;
    box-sizing: border-box;
    container-type: inline-size;
    container-name: service-embla;
    /* loop + gap は「3枚目↔1枚目」に隙間が出ないため、公式の padding 方式（旧 gap-4 = 1rem） */
    --service-slide-spacing: 1rem;
  }
  .service-embla__container {
    backface-visibility: hidden;
    touch-action: pan-y pinch-zoom;
    margin-left: calc(var(--service-slide-spacing) * -1);
  }
  /* cqi = ビューポートのコンテンツ幅。カードを少し細くして中央時に左右に覗きと余白を確保 */
  .service-embla__slide {
    flex: 0 0 min(300px, calc(100vw - 10rem));
    min-width: 0;
    padding-left: var(--service-slide-spacing);
  }
  @supports (width: 1cqi) {
    .service-embla__slide {
      flex: 0 0 min(300px, calc(100cqi - 4rem));
    }
  }
</style>

<script>
  // @ts-nocheck — Embla は動的 import、IDE は .astro 内 script を TS として解釈しないことがある
  const MQ = '(min-width: 768px)';
  let emblaApi = null;

  function destroyEmbla() {
    const vp = document.getElementById('service-embla-viewport');
    if (emblaApi) {
      emblaApi.destroy();
      emblaApi = null;
    }
    if (vp) delete vp.dataset.emblaInit;
  }

  async function initEmbla() {
    const vp = document.getElementById('service-embla-viewport');
    if (!vp || vp.dataset.emblaInit === '1') return;
    if (window.matchMedia(MQ).matches) return;

    const { default: EmblaCarousel } = await import('embla-carousel');
    if (window.matchMedia(MQ).matches) return;
    if (vp.dataset.emblaInit === '1') return;

    vp.dataset.emblaInit = '1';

    const api = EmblaCarousel(vp, {
      loop: true,
      align: 'center',
      dragFree: false,
      skipSnaps: false,
      watchDrag: true,
      watchResize: true,
    });
    emblaApi = api;
    // 全幅化・フォント後の計測ずれで左が詰むことがあるため 1 フレーム後に再計測
    requestAnimationFrame(() => {
      if (emblaApi === api) api.reInit();
    });
  }

  function apply() {
    if (window.matchMedia(MQ).matches) {
      destroyEmbla();
    } else {
      void initEmbla();
    }
  }

  const mq = window.matchMedia(MQ);
  mq.addEventListener('change', apply);

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', apply, { once: true });
  } else {
    apply();
  }
</script>
